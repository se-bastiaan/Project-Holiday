<resources>

    <!-- Base application theme. -->
    <style name="Theme.ProjectHoliday" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.ProjectHoliday.ToggleButton" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/primary</item>
    </style>

    <style name="Theme.ProjectHoliday.PlanButton" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/primary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>


</resources>
